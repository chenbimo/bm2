# v.no.version

## 修复

- 修复 bm2 与 bm2d 版本不一致时报出看不懂的错误（现直接提示执行 `bm2 reload`）
- 修复已注册实例较多（约 300 个以上）时 `bm2 list` 直接报错
- 修复运行时无响应时 `bm2 start` 会卡住整个 bm2d（现改为 5 秒超时并给出提示）
- 修复 CLI 异常退出后 bm2d 可能卡在响应写入、不再应答其他请求
